const form=document.getElementById('chatForm'),input=document.getElementById('input'),messages=document.getElementById('messages');
function add(text,kind){const el=document.createElement('div');el.className='message '+kind;el.textContent=text;messages.appendChild(el);messages.scrollTop=messages.scrollHeight}
form.addEventListener('submit',async e=>{e.preventDefault();const text=input.value.trim();if(!text)return;add(text,'user');input.value='';const wait=document.createElement('div');wait.className='message ai';wait.textContent='QuantumAI denkt …';messages.appendChild(wait);try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text})});const d=await r.json();wait.remove();add(d.response||d.error||'Keine Antwort erhalten.','ai')}catch(_){wait.remove();add('Verbindung zur QuantumAI fehlgeschlagen.','ai')}});