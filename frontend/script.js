const form=document.getElementById('chatForm');const input=document.getElementById('input');const messages=document.getElementById('messages');form.addEventListener('submit',e=>{e.preventDefault();const text=input.value.trim();if(!text)return;add(text,'user');input.value='';setTimeout(()=>add('Ich bin als statischer Demo-Assistent verfügbar. Für echte KI-Antworten muss dieses Frontend mit einem sicheren Backend/API verbunden werden.','ai'),250)});function add(text,kind){const el=document.createElement('div');el.className=`message ${kind}`;el.textContent=text;messages.appendChild(el);messages.scrollTop=messages.scrollHeight}